
@{
    ViewData["Title"] = "consumerleadinfo";
    Layout = "~/Views/Layouts/_adminLayout.cshtml";
}
<link href="//cdn.datatables.net/1.10.7/css/jquery.dataTables.min.css" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
<link rel="stylesheet" type="text/css" href="https://ajax.aspnetcdn.com/ajax/jquery.dataTables/1.9.0/css/jquery.dataTables_themeroller.css">
<link rel="stylesheet" type="text/css" href="https://ajax.aspnetcdn.com/ajax/jquery.dataTables/1.9.0/css/jquery.dataTables.css">
<script type="text/javascript" charset="utf8" src="https://ajax.aspnetcdn.com/ajax/jquery.dataTables/1.9.0/jquery.dataTables.min.js"></script>
<script type="text/javascript" charset="utf8" src="http://code.jquery.com/jquery-1.8.3.min.js"></script>


<link href="https://cdn.datatables.net/select/1.1.2/css/select.dataTables.min.css" rel="stylesheet" />




@*<script src="https://code.jquery.com/jquery-3.5.1.js"></script>
    <script src="https://cdn.datatables.net/1.10.21/js/jquery.dataTables.min.js"></script>
    <style>
        table.dataTable.select tbody tr,
        table.dataTable thead th:first-child {
            cursor: pointer;
        }
    </style>*@

<style>
    .fapencil {
        background: url('../img/edit.png') !important;
        background-repeat: no-repeat !important;
        color: transparent;
    }

    a:hover {
        color: transparent !important;
    }
    .yesbtn {
        padding: 10%;
        color: white;
        width: 100%;
        border-radius: 8px;
        background-color: #c53030;
        border: 0;
        font-size: 20px;
    }
    .yesbtn:hover {
      
        background-color: #2c7a7b;
       
    }
    .nobtn {
        padding: 2% 0% 2% 0%;
        color: white;
        width: 100%;
        border-radius: 8px;
        background-color: #2398dc;
        border: 0;
        font-size: 20px;
    }
        .nobtn:hover {
            background-color: #2c7a7b;
        }
    td {
        background-color: white !important;
        padding-top: 2% !important;
        padding-bottom: 2% !important;
    }

    table {
        border-collapse: collapse;
    }

    table {
        border-collapse: collapse;
    }

        table.dataTable thead th {
            border-bottom: 2px solid #f7f7ff !important;
        }

    thead, th, td {
        border-bottom: 2px solid #f7f7ff;
    }

    table.dataTable.no-footer {
        border-bottom: 2px solid #f7f7ff;
    }

    thead {
        background-color: #f7f7ff !important;
    }

    table.dataTable thead th {
        /* padding-bottom:10px;
        padding-top:10px;*/
        padding-top: 1% !important;
        padding-bottom: 1% !important;
        background-color: #f7f7ff !important;
    }

    .mytable_previous a {
        border: none !important;
        padding-left: 100% !important;
    }

    .hundered {
        padding: 1% !important;
        color: white !important;
        background-color: #38A169 !important;
        border-radius: 2px !important;
    }

    .danger {
        padding: 1% !important;
        color: white !important;
        background-color: #718096 !important;
        border-radius: 2px !important;
    }

    .mytable_next a {
        border: none !important;
    }


    tr.border-bottom {
        border-bottom: 1px solid #ff000d !important;
    }

    tr {
        border-bottom: 2px solid #f7f7ff !important;
    }

        tr.border_bottom td {
            border-bottom: 1px solid black;
        }


    .cancel {
        padding: 2% 0% 2%;
        background-color: transparent;
        border-color: lightgray;
        width: 16em;
        border-width: 2px;
    }

    .swal2-actions {
        display: none;
    }
</style>






@*<style>
       .pagination {
            display: none !important;
        }

        .pagination li.Prev, .pagination Next {
            display: inline !important;
        }
    </style>*@

<div class="row">

    <div class="col-md-6">
      
        <br>
        <div class="col-md-5">
            <label style="margin-bottom: 5px;">SELECT OFFERS:</label>
            <select id="change" onchange="redirect();" class="form-control">
                <option value="volvo">SELECT OFFERS </option>
                <option value="CO-REGO">CO-REG OFFERS</option>
                <option value="WALL">WALL OFFERS</option>
                <option value="PATH OFFERS">PATH OFFERS</option>
                <option value="POP OFFERS">POP OFFERS</option>
                <option value="EXIT OFFERS">EXIT OFFERS</option>
               
            </select>
        </div>

    </div>


 
</div>
<form class="mt-3" id="Form">


    <input style="display:none;" id="offertypee" name="offertype" value="coregg" />
    <div class="col-12" id="POP">
        <h4 class="mt-0 header-title h3bg">POP OFFERS</h4>

        <div class="card" style="margin-top: -7px; box-shadow: 0 9px 4px 0 rgb(0 0 0 / 20%), 0 6px 20px 0 rgb(0 0 0 / 19%); ">
            <div class="card-body">


                <table id="mytable" class="mytable" style="width:100%">
                    <thead data-orderable="false">
                        <tr data-orderable="false">
                            <th data-orderable="false"></th>
                            <th data-orderable="false"><input name="select_all" value="1" type="checkbox" autocomplete="off"></th>
                            <th data-orderable="false"><i class="fa fa-user" aria-hidden="true"></i>&nbsp;IMAGE</th>
                            <th data-orderable="false"><i class="fa fa-lock" aria-hidden="true"></i>&nbsp;OFFER TITLE</th>
                            <th data-orderable="false"><i class="fa fa-lock" aria-hidden="true"></i>&nbsp;UPDATES</th>
                            <th data-orderable="false"><i class="fa fa-lock" aria-hidden="true"></i>&nbsp;PLACEMENT</th>
                            <th data-orderable="false">ACTION</th>
                            <th data-orderable="false"><i class="fa fa-exclamation-circle" aria-hidden="true"></i>&nbsp;STATUS</th>



                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var i in ViewBag.popffers)
                        {
                            <tr>
                                <td></td>
                                <td><input type="checkbox" class="archive" value="@i.PopUpId" name="archiveitem" autocomplete="off"></td>
                                <td>
                                    <img src="~/UploadedImages/@i.Image" width="48" height="48" class="tableimg" alt="">

                                </td>
                                <td>

                                    <a>@i.OfferName </a>

                                </td>
                                <td>

                                  <a>@i.Date EST</a>

                                </td>
                                <td>

                                    <a>@i.Priority</a>

                                </td>

                                <td><a style=" cursor: pointer;" onclick="offerdetail('@i.PopUpId','popffers')">....</a> <a> @Html.ActionLink("jds ", "update_offer", "admin", null, null, "", new { tid = i.PopUpId, offertype = "popffers", click = "alloffer" }, new { @class = "  fa fapencil text-inverse m-r-10", title = "Edit" })</a>  <a style=" cursor: pointer;" onclick="showconfirmationmodal('popups');">×</a></td>
                                <td>

                                    <a class="hundered">Active</a>

                                </td>

                            </tr>

                        }




                    </tbody>

                </table>


            </div>


        </div>
    </div>



    <div class="modal fade bd-example-modal-lg" id="modalconfirm" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true" style="top:10%">
        <div class="modal-dialog modal-lg">

            <div class="modal-content" style="margin-top:10em; background: linear-gradient(90deg, #1E292B 0%, #273132 20.83%, #313735 40.62%, #3E413A 60.94%, #4A4B43 80.21%, #5A5748 100%);">
                <div class="modal-header" style="background-color: white;">
                    <h5 class="modal-title" id="exampleModalLabel">Are you sure you want to delete this offer?</h5>
                    <button onclick="modalclose()" type="button" class="close" data-dismiss="modal" aria-label="Close" style="background-color: transparent;border: 0;font-size: 2.10em;">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="row">
                        <div class="col-2">
                            <button type="button" onclick="delcheckbox() " class="yesbtn">Yes</button>
                        </div>
                        <div class="col-10">
                            <button type="button" onclick="selectedcheckbox() " class="nobtn">No,Move it to Archive</button>
                        </div>

                    </div>

                </div>
            </div>
        </div>
    </div>


    <div class="modal fade bd-example-modal-lg" id="modal" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">

            <div class="modal-content" style="margin-top:10em; background: linear-gradient(90deg, #1E292B 0%, #273132 20.83%, #313735 40.62%, #3E413A 60.94%, #4A4B43 80.21%, #5A5748 100%);">
                <div class="col-md-12">
                    <button onclick="modalclose()" style="float: right; background-color: white;" type="button" class="btn-close"></button>

                </div>
                <div class="row" style="padding-top: 5%; padding-bottom: 5%; ">

                    <div class="col-md-5" style="line-height: 28px; padding-left: 5%;  font-size: 18px;color:white;">

                        <div>OFFER TITLE:</div>
                        <div>AFFLIATE ID:</div>
                        <div>OFFER ID:</div>
                        <div>DSCRIPTION 1:</div>
                        <div>DSCRIPTION 2:</div>
                        <div>BUDGET:</div>
                        <div>BUDGET DURATION:</div>
                        <div>LEAD CAP:</div>
                        <div>LEAD CAP DURATION:</div>

                    </div>


                    <div class="col-md-7" style="text-align:end;padding-right: 5%;font-size: 15px;color:white;">
                        <div class="ot"></div>

                        <div style="height: 1.9em;" id="affliateid">None</div>
                        <div style="height: 1.9em;" id="offerid">None</div>
                        <div style="height: 1.9em;" id="discription_A">None</div>
                        <div style="height: 1.9em;" id="discription_B">None</div>
                        <div style="height: 1.9em;" id="budget">None</div>
                        <div style="height: 1.9em;" id="budgetduration">None</div>
                        <div style="height: 1.9em;" id="leadcap">None</div>
                        <div style="height: 1.9em;" id="leadcapduration">None</div>

                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="modal" id="confirmmodal" tabindex="-1" role="dialog" style="margin-top: 23em;">
        <div class="modal-dialog" role="document">
            <div class="modal-content">

                <div class="modal-body">
                    <div class="row">
                        <div class="col-md-4"></div>
                        <div class="col-md-4">

                            <h1 style="background-color: #FED7E2; margin-left: 1em; background-color: #FED7E2; width: 48px; height: 48px; padding-top: 0%; padding-left: 10%; border-radius: 87px; color: hotpink; ">×</h1>

                        </div>
                        <div class="col-md-4"></div>

                    </div>
                    <h2 style=" text-align: center; font-size: 24px;">Remove</h2>
                    <p style=" text-align: center;">You’ve officially removing something this user. All the access will be removed immediately.</p>

                    <div class="row">
                        <div class="col-md-6">
                            <button onclick="cancel()" type="button" style=" padding: 2% 0% 2%; background-color: transparent; border-color: lightgray; width: 16em; border-width: 2px;">Cancel</button>
                        </div>
                        <div class="col-md-6">
                            <button type="button" style=" padding: 2% 0% 2%; color: white; background-color: #319795; border-color: lightgray; width: 16em; border-width: 2px; " data-dismiss="modal">Continue</button>
                        </div>

                    </div>
                </div>

            </div>
        </div>
    </div>
</form>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@9"></script>
<script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@10/dist/sweetalert2.all.min.js"></script>
<script>
    function showmodal() {


    }
    function modalclose() {

        $("#modal").modal('hide');
    }
    function showconfirmationmodal(offer) {
       
        $("#offertypee").val(offer);
     
        $("#modalconfirm").modal('show');
        
       
        /*$("#confirmmodal").modal('show');*/
        //Swal.fire({
        //    icon: '  <h1 style="background-color: #FED7E2; margin-left: 1em; background-color: #FED7E2; width: 48px; height: 48px; padding-top: 0%; padding-left: 10%; border-radius: 87px; color: hotpink; ">×</h1>',
        //    title: '  <h1 style="background-color: #FED7E2;  background-color: #FED7E2; width: 48px; height: 48px; padding-top: 0%;  border-radius: 87px; color: hotpink; ">×</h1>',
        //    text: 'Remove ',
        //    html: "<h2 style='text- align: center; font - size: 24px;'onclick='cancel()'>Remove</h2><br/><p style='color:black;font-size: 14px;' >You’ve officially removing something this user. All the access will be removed immediately.</p><br/> <button onclick='cancel()' type='button' style='  height: 1.9em;padding:2% 0 % 2 %; background-color: transparent; border-color: lightgray; width: 11em; float: left;; border - width: 2px;'>Cancel</button>    <button type='button' style=' height: 1.9em; padding:2% 0 % 2 %; color: white; background-color: #319795; border-color: lightgray; width: 11em; border-width: 2px; 'onclick='selectedcheckbox()' data-dismiss='modal'>Continue</button>",




        //    //showCancelButton: true,
        //    //confirmButtonColor: "#319795",
        //    //confirmButtonText: "Cancel",
        //    //closeOnConfirm: false,


        //})
    }
    function cancel() {
        Swal.close();
        $("#confirmmodal").modal('hide');
    }
    function delcheckbox(offer) {
        $("#modalconfirm").modal('hide');
        var formData = {};
        var formData = $("#Form").serialize();
       
        $.ajax(
            {
                type: "POST",
                url: '/admin/Deleteconumeroffer',

                contentType: 'application/x-www-form-urlencoded; charset=UTF-8', // when we use .serialize() this generates the data in query string format. this needs the default contentType (default content type is: contentType: 'application/x-www-form-urlencoded; charset=UTF-8') so it is optional, you can remove it
                data: formData,
                success: function (result) {
                    deletesumitsweett();
                    setTimeout(function () {
                        location.reload(true);

                    }, 1000);

                },
                error: function () {
                    alert('Failed to receive the Data');
                    console.log('Failed ');
                }

            }
        );
    }
    function selectedcheckbox(offer) {
        $("#modalconfirm").modal('hide');
        var formData = {};
        var formData = $("#Form").serialize();
     
        $.ajax(
            {
                type: "POST",
                url: '/admin/archieveconumeroffer',

                contentType: 'application/x-www-form-urlencoded; charset=UTF-8', // when we use .serialize() this generates the data in query string format. this needs the default contentType (default content type is: contentType: 'application/x-www-form-urlencoded; charset=UTF-8') so it is optional, you can remove it
                data: formData,
                success: function (result) {
                    sumitsweett();
                    setTimeout(function () {
                        location.reload(true);

                    }, 1000);

                },
                error: function () {
                    alert('Failed to receive the Data');
                    console.log('Failed ');
                }

            }
        );
    }
    function deletesumitsweett() {
        var icon = "success";
        if (status == "success") {
            icon = "success";
        }

        var config = {}
        config.icon = icon;
        config.title = "Success";
        config.text = "Offer Deleted SuccessFully";

        Swal.fire(config);
     

    }
    function sumitsweett() {
        var icon = "success";
        if (status == "success") {
            icon = "success";
        }

        var config = {}
        config.icon = icon;
        config.title = "Success";
        config.text = "Offer Archive SuccessFully";

        Swal.fire(config);
       

    }





    $(document).ready(function () {

        /** Handling checboxes selection*/

        function updateDataTableSelectAllCtrl(table) {
            var $table = table.table().node();
            var $chkbox_all = $('tbody input[type="checkbox"]', $table);
            var $chkbox_checked = $('tbody input[type="checkbox"]:checked', $table);
            var chkbox_select_all = $('thead input[name="select_all"]', $table).get(0);

            // If none of the checkboxes are checked
            if ($chkbox_checked.length === 0) {
                chkbox_select_all.checked = false;
                if ('indeterminate' in chkbox_select_all) {
                    chkbox_select_all.indeterminate = false;
                }

                // If all of the checkboxes are checked
            } else if ($chkbox_checked.length === $chkbox_all.length) {
                chkbox_select_all.checked = true;
                if ('indeterminate' in chkbox_select_all) {
                    chkbox_select_all.indeterminate = false;
                }

                // If some of the checkboxes are checked
            } else {
                chkbox_select_all.checked = true;
                if ('indeterminate' in chkbox_select_all) {
                    chkbox_select_all.indeterminate = true;
                }
            }
        }

        /** Responsive table with colreorder, fixed header;footer, sortable teble*/
        var table = $('.mytable').DataTable({

            responsive: {
                details: {
                    type: 'column'
                }
            },
            /*  order: [1, 'asc'],*/
            //colReorder: {
            //    fixedColumnsLeft: 1,
            //    fixedColumnsLeft: 2
            //},
            //fixedHeader: {
            //    header: true,
            //    footer: true
            //},
            'columnDefs': [{
                'targets': 1,
                'searchable': false,
                'orderable': false,
                'width': '1%',

                'className': 'dt-body-center',

            },
            {
                'className': 'control',
                'orderable': false,
                targets: 0,

            },
            ],


            'filter': false,

            "dom": '<"bottom"i>rt<"bottom"flp><"clear">',

            "language": {
                "info": '<"bottom"i>rt<"bottom"flp><"clear">',
                "info": " _START_ - _END_ of _TOTAL_",
                "paginate": {
                    "next": '<img src="/img/nextt.png" style=" width: 15px;">',

                    "previous": '<img src="/img/back.png" style=" width: 15px;" > '
                }
            },

            "pagingType": "simple",
            /*'order': [[1, 'asc']],*/
            'rowCallback': function (row, data, dataIndex) {
                // Get row ID
                var rowId = data[6];

                // If row ID is in the list of selected row IDs
                if ($.inArray(rowId, /**rows_selected */) !== -1) {
                    $(row).find('input[type="checkbox"]').prop('checked', true);
                    $(row).addClass('selected');
                }
            }
        });

        /** mark single checkboxes */

        $(document).ready(function () {
            // Array holding selected row IDs
            var rows_selected = [];


            // Handle click on checkbox
            $('.mytable tbody').on('click', 'input[type="checkbox"]', function (e) {
                var $row = $(this).closest('tr');

                // Get row data
                var data = table.row($row).data();

                // Get row ID
                var rowId = data[6];

                // Determine whether row ID is in the list of selected row IDs
                var index = $.inArray(rowId, rows_selected);

                // If checkbox is checked and row ID is not in list of selected row IDs
                if (this.checked && index === -1) {
                    rows_selected.push(rowId);

                    // Otherwise, if checkbox is not checked and row ID is in list of selected row IDs
                } else if (!this.checked && index !== -1) {
                    rows_selected.splice(index, 1);
                }

                if (this.checked) {
                    $row.addClass('selected');
                } else {
                    $row.removeClass('selected');
                }

                // Update state of "Select all" control
                updateDataTableSelectAllCtrl(table);

                // Prevent click event from propagating to parent
                e.stopPropagation();
            });

            // Handle click on table cells with checkboxes
            $('.mytable').on('click', 'tbody td, thead th:first-child', function (e) {
                $(this).parent().find('input[type="checkbox"]').trigger('click');
            });

            // Handle click on "Select all" control
            $('thead input[name="select_all"]', table.table().container()).on('click', function (e) {
                if (this.checked) {
                    $('.mytable tbody input[type="checkbox"]:not(:checked)').trigger('click');
                } else {
                    $('.mytable tbody input[type="checkbox"]:checked').trigger('click');
                }

                // Prevent click event from propagating to parent
                e.stopPropagation();
            });

            // Handle table draw event
            table.on('draw', function () {
                // Update state of "Select all" control
                updateDataTableSelectAllCtrl(table);
            });

            // Handle form submission event
            $('#frm-mytable').on('submit', function (e) {
                var form = this;

                // Iterate over all selected checkboxes
                $.each(rows_selected, function (index, rowId) {
                    // Create a hidden element
                    $(form).append(
                        $('<input>')
                            .attr('type', 'hidden')
                            .attr('name', 'id[]')
                            .val(rowId)/**there are data from json file or db*/
                    );
                });

                $('#examplee-console').text($(form).serialize());
                console.log("Form submission", $(form).serialize());

                // Remove added elements
                $('input[name="id\[\]"]', form).remove();

                // Prevent actual form submission
                e.preventDefault();
            });

        });



        /** Handling responsive expand collapse all*/



        // Handle click on "Expand All" button
        $('#btn-show-all-children').on('click', function () {
            // Expand row details
            table.rows(':not(.parent)').nodes().to$().find('td:first-child').trigger('click');
        });

        // Handle click on "Collapse All" button
        $('#btn-hide-all-children').on('click', function () {
            // Collapse row details
            table.rows('.parent').nodes().to$().find('td:first-child').trigger('click');
        });
    });

    var table = $('.mytable').DataTable();

    $('.mytable tbody').on('click', 'tr', function () {
        console.log(table.row(this).data());
        alert(table.row(this).data());
    });
</script>

<script>

    function offerdetail(count, offer) {

        $("#modal").modal('show');
        var id = count;
        var type = offer;

        $('.flightdetail').hide();
        $('#btnaddflight').hide();
        $('#purposalform').hide();
        $.ajax
            (
                {
                    type: "GET",
                    url: "/admin/getmoreoffer",
                    contentType: "Application/json;charset=utf-8",
                    data: { id: count, offertype: type },
                    success: function (data) {
                        console.log("result", data);
                        $(".ot").text(data[0].offer_title);
                        $(".ot").text(data[0].offer_Name);
                        $("#affliateid").text(data[0].affliateid);
                        $("#archive").text(data[0].archive);
                        $("#budget").text(data[0].budget);
                        $("#budgetduration").text(data[0].budgetduration);
                        $("#discription_A").text(data[0].discription_A);
                        $("#discription_B").text(data[0].discription_B);
                        $("#leadcap").text(data[0].leadcap);
                        $("#leadcapduration").text(data[0].leadcapduration);
                        $("#lower_title").text(data[0].lower_title);
                        $("#offerid").text(data[0].offerid);
                        $("#validation").text(data[0].validation);

                    },


                }
            );
    }
</script>

<script>

    
    //$(document).ready(function () {
       
    //    $('#CO-REG').show();
    //    $('#WALL').hide();
    //    $('#PATH').hide();
    //    $('#POP').hide();
    //    $('#Unclaimed').hide();
    //    $('#EXIT').hide();
    //});
    function redirect() {

        var web = $("#change").val();
        if (web == "CO-REGO") {
            window.location.assign("/admin/coreg_offers")
            //$('#WALL').hide();
            //$('#PATH').hide();
            //$('#POP').hide();
            //$('#Unclaimed').hide();
            //$('#EXIT').hide();
        }
        if (web == "WALL") {
            window.location.assign("/admin/wall_offers")
            //$('#CO-REG').hide();

            //$('#WALL').show();
            //$('#PATH').hide();
            //$('#POP').hide();
            //$('#Unclaimed').hide();
            //$('#EXIT').hide();

        }
        if (web == "PATH OFFERS") {
            window.location.assign("/admin/path_offers")
            //$('#CO-REG').hide();

            //$('#WALL').hide();
            //$('#PATH').show();
            //$('#POP').hide();
            //$('#Unclaimed').hide();
            //$('#EXIT').hide();
        }
        if (web == "POP OFFERS") {

            window.location.assign("/admin/pop_offers")
            //$('#CO-REG').hide();

            //$('#WALL').hide();
            //$('#PATH').hide();
            //$('#POP').show();
            //$('#Unclaimed').hide();
            //$('#EXIT').hide();
        }

        if (web == "EXIT OFFERS") {
            window.location.assign("/admin/exit_offers")
            //$('#CO-REG').hide();

            //$('#WALL').hide();
            //$('#PATH').hide();
            //$('#POP').hide();
            //$('#Unclaimed').hide();
            //$('#EXIT').show();
        }


    }


</script>
